.videos-new
  .videos-new-wrapper
    .videos-new-wrapper__head
      %h1.videos-new-wrapper__head--text
        = icon("fas", "video")
        VIDEO
      %p Connect Vimeo to offer secure videos to your patrons or add a URL.

    = form_with model: @video, id: 'video-new-form' do |f|
      - if @video.errors.any?
        #error_explanation
          %h2
            = pluralize(@video.errors.count, "error")
            prohibited this article from being saved:
          %ul
            - @video.errors.full_messages.each do |msg|
              %li= msg

      = f.file_field :video, :accept => 'video/*'
      = f.submit value: "Continue"