.user-show-contents
  .user-show-wrapper
    = render 'nav'
    = render 'introduction'
    -# = render 'plan'
    = render 'perk'
    = render 'sns'
    = render 'about'
    = render 'goal'
    = render 'appeal'
    = link_to "see all items", creator_path(@creator.id)